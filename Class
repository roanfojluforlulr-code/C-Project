#include <iostream>
using namespace std;

class Rectangle {
    int w, h;
public:
    Rectangle(int w, int h): w(w), h(h) {}
    int area() { return w*h; }
};

int main() {
    Rectangle r(4, 5);
    cout << r.area();
}
